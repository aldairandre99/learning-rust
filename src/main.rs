#![cfg_attr(debug_assertions, allow(dead_code))]
mod prints;
mod var;
mod types;
mod strings;
mod tuples;

fn main() {
    //prints::run();
    //var::run();
    //types::run();
    //strings::run();
    tuples::run();
}
